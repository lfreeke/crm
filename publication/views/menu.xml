<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record
        id="action_subscription"
        model="ir.actions.act_window"
        >
        <field name="view_mode">tree,form</field>
        <field name="res_model">publication.subscription.address</field>
        <field name="name">Subscription Addresses</field>
    </record>

    <menuitem
        id="menu_subscription"
        action="action_subscription"
        name="Subscriptions"
        parent="sales_team.menu_sales"
        sequence="60"
        />

    <record
        id="action_publication"
        model="ir.actions.act_window"
        >
        <field name="view_mode">tree,form</field>
        <field name="res_model">publication.publication</field>
        <field name="name">Publications</field>
    </record>

    <menuitem
        id="menu_publication"
        action="action_publication"
        name="Publications"
        parent="sales_team.menu_sales"
        sequence="70"
        />

</odoo>
